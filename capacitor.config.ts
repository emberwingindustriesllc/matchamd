import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.emberwingindustriesllc.matchamd',
  appName: 'MatchaMD',
  webDir: 'dist'
};

export default config;
